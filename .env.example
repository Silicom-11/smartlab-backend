# ========================================
# SMARTLAB BACKEND - Variables de Entorno
# ========================================

# ========================================
# Server Configuration
# ========================================
PORT=5000
NODE_ENV=production

# ========================================
# MongoDB Atlas Configuration
# ========================================
# 1. Ve a MongoDB Atlas: https://cloud.mongodb.com/
# 2. Database -> Connect -> Connect your application
# 3. Copia la connection string y reemplaza <password> y <dbname>
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/smartlab?retryWrites=true&w=majority&appName=SmartLab

# ========================================
# JWT Configuration
# ========================================
# Genera una clave secreta ÚNICA para producción
# Comando: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CAMBIA_ESTO_POR_UNA_CLAVE_SECRETA_SUPER_LARGA_Y_SEGURA
JWT_EXPIRES_IN=24h

# ========================================
# CORS Configuration
# ========================================
# Para desarrollo: http://localhost:5173
# Para producción: Agrega tu dominio de Render y frontend
# Ejemplo: https://smartlab-backend.onrender.com,https://smartlab-web.vercel.app
ALLOWED_ORIGINS=http://localhost:5173
FRONTEND_URL=http://localhost:5173

# ========================================
# IMPORTANTE PARA RENDER.COM:
# ========================================
# En Render, configura estas variables en:
# Dashboard -> Tu Servicio -> Environment
# 
# Variables OBLIGATORIAS:
# - MONGODB_URI (tu connection string de MongoDB Atlas)
# - JWT_SECRET (genera uno nuevo y seguro)
# - NODE_ENV=production
# - ALLOWED_ORIGINS (agrega tu URL de Render y frontend)
# 
# Render detectará automáticamente el PORT
# ========================================
